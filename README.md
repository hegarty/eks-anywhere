# EKS-Anywhere

Full EKS Anywhere cluster locally using Docker, complete with Prometheus + Grafana monitoring

## Prerequisites
- Docker
- Homebrew:
  ```bash
  brew install docker kubectl eksctl kind helm
  brew install eks-anywhere
```
